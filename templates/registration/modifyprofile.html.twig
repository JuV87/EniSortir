{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}

   <h2>Modifier votre profil : </h2>

        {{ form_start(recoveryForm) }}
        {{ form_widget(recoveryForm) }}
        <br>
        <button class="btn btn-primary">Valider votre modification !</button>
        <br>
        <br>
        <a href="{{ path('user_modify_password') }}" title="ChangePassword" alt="">Changer le mot de passe</a>
        {{ form_end(recoveryForm) }}

{% endblock %}
