{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
     <br><br>
<div class="container col-sm-10 col-xl-6 justify-content-center" >
   <h2>Modifier votre profil : </h2>

        {{ form_start(recoveryForm) }}
        {{ form_widget(recoveryForm) }}
        <br>
        <button class="btn btn-primary">Valider votre modification !</button>
        <br>
        <br>
        <a href="{{ path('modify_password') }}" title="ChangePassword" alt="">Changer le mot de passe</a>
        {{ form_end(recoveryForm) }}
</div>
{% endblock %}
